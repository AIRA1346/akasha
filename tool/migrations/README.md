# tool/migrations

완료된 스키마·데이터 마이그레이션 스크립트 보관용입니다.  
현재는 루트 `tool/`에 남아 있는 파일들을 점진적으로 이곳으로 옮깁니다.

이동 대상 예: `migrate_registry_v3.dart`, `migrate_shards_v3_to_v4_hash.dart`, `sync_legacy_works_registry.dart`
